# 更新日志

只记当前版本。更早的改动看 git 历史或 Release 页。

## v1.9.1 — 日志规范修复

- `social/generator.py`、`social/persona.py`：删掉非 AstrBot 环境下的 `logging.getLogger`
  兜底分支，logger 改为直接 `from astrbot.api import logger`，与插件其余文件保持一致。
  插件本就只在 AstrBot 内运行，该兜底分支永远不会被走到，行为无任何变化。

## v1.9.0 — 更频繁的默认节奏；刚装就能认识人（历史导入 + 播种名单）

两条主线，都是针对「装完就默默没人、想找人得先跟她说句话才认识她」的反馈。

### 节奏整体往前顶一档（默认值变化，老用户自动跟随）

- `activity_level` 55 → 65：整体想说活儿的程度再顶一格（×1.36 → ×1.48）
- `urge_refill_hours` 3 → 2（下限）：最在意的人攒满一次念头的时间再压一半
- `user_cooldown_minutes` 90 → 45、`global_cooldown_minutes` 20 → 10、`skip_cooldown_minutes` 25 → 15：
  护栏同步放低，不然念头攒得快、冷却拦得紧，频率照旧
- 老用户跑着跑着要跟着新版节奏走：`migrate_legacy_defaults` 的旧默认值表里补了
  1.8.x 的这五个值，装过 1.8.x 的会在升级后首启时被一次性提升（自己改过的值不动）
- 面板里所有选项的描述重写了一遍：每个参数「管什么、和谁叠加、调了之后会发生什么」
  都写明白，不再是一句短语

### 播种：不靠「先聊过」也认识人

新增 `social/history_ingest.py`，引擎启动与每半小时跑一次 `_seed_tick`：

- **历史导入（`history_ingest`，默认开）**：只读打开 AstrBot 会话库
  （`data/data_v4.db`，旧 4.x 的 `data_v3.db` 也能认）的 conversations 表，把装插件
  之前就聊过的人导进 state.json：umo、聊过多少条、最后活跃时间。导入的人从
  「最后聊天的时刻」开始攒念头——三天前聊过的人，攒不满三个小时就能被主动找上。
  已在 state.json 里的人一律不动，实时数据永远比旧快照新；群会话按 private_only
  过滤；store_message_text 关着时连「最后一句」也不抄。
- **播种名单（`seed_users` + `seed_platform`，默认空）**：连历史都没有的人也能写进
  名单，她攒够念头会主动找上门——不靠任何聊过的启发式。名单里的人攒念头速度
  自动打五折（`SEED_URGE_SCALE=0.5`，素未谋面开口本来就比熟人慢），被冷落够多次后
  天花板机制自然把她压下去，不会变成骚扰。
- 多 bot 部署时播种进 "default" 名下，第一条真实消息进来 `migrate_default_bot`
  会把这些记录搬到真实 bot_id 下；单 bot 时直接归那个角色。

状态面板新增「播种：历史导入开/关　名单 N 人（没聊过也能找）」一行；
「触发社交」在没人可找时会给出具体的补救入口。

## v1.8.2 — 信号文件启动即写、每周期刷心跳（修「看起来没对接」）

`humanoid_signals.json` 是写回给 Humanoid Core 的信号（刚替谁主动开过口、被谁冷落了几次、
现在多想说话）。Core 那边**只读**这个文件，并且超过 15 分钟不更新就不采信。

问题出在写入时机：三个 setter 都在「值没变」时直接 return，而 `ignored_streak` 一直是 0、
`desire` 在没读到 Core 契约时是 `None`——于是**这个文件永远不会被创建**。装得好好的社交层，
在 `/拟人诊断` 里只显示一句「没读到 humanoid_signals.json（未装自主拟人社交，或它还没写过）」，
跟「真没装」长得一模一样。就算它发过一条主动消息、文件建起来了，之后没有东西去刷 mtime，
15 分钟后 Core 照样把它当过期丢掉。

- `social/signals.py`：新增 `beat()`——内容没变时只按心跳（120s）刷新 mtime，文件不存在就
  立刻建出来。心跳逻辑本来就在 `flush()` 里，只是没人调它。
- `social/engine.py`：`start()` 里先 `beat()` 一次（`mode=standalone` 时不写，那种模式不联动）；
  每轮 `try_once()` 写完 setter 再 `beat()` 一次。写失败不影响发送，只记一条日志。

现在装完启动就有文件，Core 的诊断会显示
「社交层信号：读到并在用。上次刷新 7s 前…（看的是 <路径>）」——Core v2.16.5 起会把
「没装 / 装了没写过 / 信号超时 / 在用」四种情况分开说，并把它去看的路径打出来。

配合的 Core 侧改动在 humanoid_core v2.16.5。

## v1.8.1 — 修一个把插件堵死的括号

v1.8.0 的出站记账钩子写成了 `@after_message_sent`（无括号）。AstrBot 的
`register_after_message_sent(**kwargs)` 只收关键字参数，裸用等于把函数当位置参数递进去，
**插件模块在 import 阶段就抛 `TypeError: register_after_message_sent() takes 0 positional
arguments but 1 was given`**。管理面板上看到的就是「安装失败 / 更新失败」，而插件目录已经
落地了，之后再传 zip 会一直撞「目录已存在」——不卸干净就再也装不回去。

1.8.1 只改 `main.py:175` 这一行（补上括号，AstrBot 自带插件 `builtin_stars/astrbot/main.py:338`
就是这个写法），其余代码与 1.8.0 逐字相同。

漏检原因：入口冒烟测试里的 astrbot 桩用了一个「带括号不带括号都吃」的宽松装饰器，把真机上
必然发生的 TypeError 吐掉了。已把桩里 12 个注册装饰器全部换成从 AstrBot 源码抄来的真实签名，
现在这一处会直接测红。

## v1.8.0 — 未完话题：让她接得上自己的话

上一版把「断话跟进」做出来了，但用下来发现两件事：它不够拟人，而且它那条主路径**在实际
聊天里几乎永远不成立**。根因不是参数调得不对，是缺了一块地基——插件只看得见对方说的话，
bot 在主链路里回的每一句它都不知道。

**地基：`after_message_sent` 记账（`track_own_replies`，默认开）**

新增出站记账钩子，把 bot 自己说出去的话也写进对话账本（`dir=out`、`src=chat`），并记下
`last_spoken` / `last_spoken_text` / `last_spoken_question`。少了这一笔：

- 「最后一句是谁说的」永远是对方的——主链路一回复，最后一条就该是她的，可插件看不见，
  于是旧规则「最后一句是她自己说的就别追」形同虚设，而反过来「她会不会追问」也无从判断；
- prompt 里的「最近的对话」全是对方单方面，她写出来的话接不上自己刚才说什么，像换了个人。

命令回显（`is_model_result()` 为假）不记，插件自己主动发的那句走 `record_outgoing`，不经过
respond 阶段，因此不会重复入账。已知缺口：流式回复分段送达时框架不触发该钩子，那部分记不到。

**未完话题：三条路，都不进念头模型**

攒十几小时才开口那是「另起一个话题」该有的样子；话说到一半的事，真人以分钟和小时计。

| 路 | 什么时候成立 | 说的是 |
|---|---|---|
| `probe` 追问 | 对方最近那句几乎没带信息（「还行」「就那样」「不知道」），沉默 `probe_after_minutes`（3）分钟 | 就着**那件事**再问一句，2~12 字，不是问在不在 |
| `presence` 问在不在 | 刚才确实在来回聊，双方都不说话了，过 `followup_after_minutes`（6）分钟 | 「刚才是不是卡了」这类接话，带上下文 |
| `loop` 回访 | 对方提过一件没说结果的事（面试、复查、搬家），隔 `loop_min_hours~loop_max_hours`（2.5~14h）到点 | 「对了你面试后来咋样」，必须提到那件事 |
| `closer` 收场 | 她主动发的话 `closer_after_hours`（8）小时没人接 | 「没事 就随口一说」，不要求对方回 |

- **断点从「最后一个人说话」算起，不要求那句是对方说的**：旧版要求「最后一句必须是对方的」，
  而主链路一回复最后一条永远是她的，这条最该触发的路径于是永远不成立——就是「用户随便发
  几句，模型不回来追问」的成因。
- 但**对方那句必须还热着**（`now - last_seen <= followup_max_minutes`）：只看 `max(seen, spoken)`
  的话，她在群里或跟别人说过话就会把沉默计时重置，于是隔了半天还在追问人家早上那句。
- 「她上一句在问事」不当快速追问的理由：她问了、对方没回，隔三分钟再追就是催，那个走
  `presence` 的慢档。
- 每条都有各自的「只一次」：`thread_for` 记住为哪一次断点接过、`closer_for` 记住为哪一次
  没人回收过场、`loop` 问完就清空（否则每个心跳都会把它捾回来重问）。
- **收场最多三次**（`MAX_CLOSERS`）：收场那句会刷新 `last_sent`，没有这条会变成一个永远
  不接话的人每天被收一次场，无限循环。
- 四条路的字数区间、能不能用问句、要不要提出发点，在 `generator._MODE_NOTES` 里分开写；
  共用一段「问一句在不在」的文案会让四条路长出一模一样的句子。
- 旧版 `dangling_reason` / `dangling_meta` 删除，逻辑全部收拢到 `social/threads.py`；
  `followup_for` / `followup_at` 两个旧状态键在 `_migrate()` 里清掉。

**默认更主动，并补上老用户迁移**

`urge_refill_hours` 4→3、`activity_level` 45→55、`global_cooldown_minutes` 30→20、
`user_cooldown_minutes` 120→90、`recent_talk_minutes` 30→25、`skip_cooldown_minutes` 40→25、
`followup_after_minutes` 12→6、`followup_cooldown_minutes` 60→45。全部进了 `LEGACY_DEFAULTS`，
首次以 v1.8.0 启动会一次性提升。

**修一个死配置：`adaptive_reply_rate`**

schema 里写着「关掉后不再因被冷落而降速」，但全项目除了解析没有任何地方读它——它一直是假的。
现在接到 `desire.interest_level(weigh_reply_rate=...)` 与念头天花板上：关掉后「接不接话」与
「被冷落几次」都不再参与，一个只是最近很忙的人不会被越算越边缘。

**拟人度**

- **看得见她说过的话**：prompt 里新增「你自己上一句对TA说的是…」，接话终于是接着自己说。
- **说得出今天干了什么**：接 Core v2.15 契约新增的 `day` 段（刚做过/正在做/接下来），写进
  背景与体感句；她可以说「刚跟客户过完方案」而不是「我在休息」。
- **有情绪起伏**：新增 `_relationship_line()`，把好感/亲近/攻击性三个数翻成第一人称态度句
  （「心里对TA有点意见，不用硬装热情，可以淡、可以顶一句」）。给模型看数字只会得到复述。
- **更短更碎**：字数分布新增 2~7 字一档（真人有一大半消息就这么短），连发概率 0.3→0.42。
- **SEND 协议泄露**：`llm_gate=false` 时不要求模型输出 SEND/NO，但它养成长习惯了照样会带，
  于是真发出去过一句「SEND对了你面试后来咆样」。现在 `_clean_output` 会硬删这个前缀。
- **早上不说夜里话**：`compose_tone` 把 `early_morning` 当成夜间档，早上七点会写出
  「夜挺深了，说话轻的短的」。夜间与清晨分开仲裁。
- 「得先聊起来」的窗口从「问在不在阈值的三倍」（默认 18 分钟）改成与 `followup_max_minutes`
  同量级：两个人你一句我一句但每句隔半小时，旧值会把它判成「没在聊」，追问永远不成立。

**实测**

- 冒烟 50 项（`/tmp/threads_smoke.py`）：记账、四条路各自触发与不触发、只一次、面板真的
  显示那句话、字段落盘、旧键清理。
- 三天仿真（`/tmp/threads_sim.py`，一个会聊天的用户）：

| 配置 | 条数/天 | 构成 | 距对方最后一句（中位）| 60 分钟窗口内最多 |
|---|---|---|---|---|
| v1.8.0 默认 | 3.3 | probe 50% / urge 30% / closer 10% / presence 10% | 33 分钟 | 2 条 |
| 关掉三条路（≈v1.7.7）| 1.7 | urge 100% | 426 分钟 | 1 条 |
| 对方三天不回话 | 2.0→**0.4** | 3 天 6 条，7 天 6 条，14 天仍 6 条 | — | 1 条 |

  最后一行是重点：对一个不回话的人，她在 3 天内试过 6 次（3 次念头 + 3 次收场）之后
  就彻底停了，不是每天重新试一轮。
- 接缝 15 项（`/tmp/seam_v215.py`，两端真代码）：`day`/`persona`/时区偏移从 Core 契约传到
  社交层并真的出现在 prompt 里。

## v1.7.7 — 断话跟进 + 按她的时区看表

念头模型是对的，但它只回答一个问题：「想不想另起一个话题」。这个问题确实该慢慢想
（真人不会想到哪说到哪）。可它顺手把另一件完全不一样的事也挡了：话说到一半对方突然
没声了。这种事真人十几分钟就会问一句，而按旧的机制，它得等念头攒满——实测 10:00 聊完，
下一条要到 15:52 才出来，而且已经不是「在吗」而是另起了一个话题。

**新增一条独立快路径（不看 urge）**

- `followup_enabled`（默认开）+ `followup_after_minutes`（12）+ `followup_max_minutes`（90）：
  对方沉默落在这个区间里就跟进一句。超过 90 分钟不追（隔两小时再问「在吗」不像接话）。
- 触发条件卡得很死，就是为了不变成催：「最后一句必须是对方说的」（她说完对方没回再去追
  就是追着人）；「之前得确实聊起来过」（对方偶尔发一句就走了不算）；「对方说了要去忙/去睡」
  不追（新增 `_LEAVING_WORDS` 词典，洗澡/开会/吃饭/下班…）；「她主动发的上一条还没被回」不追。
- **同一段沉默只跟一次**（`followup_for` 记下是为哪一次沉默跟的），跨段也有
  `followup_cooldown_minutes`（60）护栏。发出去之后照旧计入 pending：对方再不回，
  后续主动消息会被「上一条没回」挡住，不会连环追问。
- 闸门与 `gate_reason` 只差一条：不管 `recent_talk_minutes`。真人追问恰恰发生在刚聊完
  之后，拿那条挡它等于把路径堵死；也不查对方作息（TA 十几分钟前还在说话，肯定醒着）。
- 写话时走 `intent=followup`：目标字数 4~14、明确允许问句（覆盖掉「大多数时候说陈述句」
  那条），模型把关时也告知「这次不是另起话题」。

**时段判断改按她所在城市的小时**

`quiet_start/quiet_end`、作息直方图、以及 prompt 里写给模型的「现在是几点」，以前全用
宿主机时钟——Core 契约里明明带了时间，桥接层也读进来了（`core_time`），但全项目零引用。
插件跑在北京时区、她设在加里宁格勒时，安静时段会差 6 小时：她的白天被当成深夜。

- 现在用契约里的 `time.utc_offset_minutes`（Core v2.14.2 导出）换算；拿不到契约或老版
  Core 没有这个字段时退回本机时钟（不是当成 UTC），可用 `use_core_clock=false` 强制退回。
- **记账与判断同一口径**：`active_hours` 直方图也按她的小时写。分开的话画像与闸门差几个
  钟头，`respect_user_rhythm` 会永远判成「这个点 TA 不玩手机」。

**默认节奏提速，并补上老用户迁移**

`urge_refill_hours` 8 → 4、`recent_talk_minutes` 45 → 30。AstrBot 更新配置只补缺不覆盖，
光改默认值对老用户无效，所以这两个进了 `LEGACY_DEFAULTS`，首次以 v1.7.7 启动会一次性提升。

**实测（12 小时仿真，10:00 聊完然后对方消失）**

| 配置 | 什么时候开口 | 说的是 |
|---|---|---|
| 念头 8h、无跟进（升级前） | 15:52 | 另起一个话题 |
| 本版默认 | **10:12** | 跟进一句在吗 |
| 念头 24h + 跟进 | **10:12** | 跟进一句在吗（证明两条路真的互不依赖） |

## v1.7.6 — Token 预算

审计结论：本插件只有**一处**会调模型——准备主动开口前的那一次「该不该说 + 说什么」
（`decide` 与 `generate` 共用同一段 prompt，不各调一次）。生产最坏情况（人设 800 字 +
5 条历史 + 完整 Core 背景）实测输入约 2100 token，输出受 `max_message_length` 限制，
合计远不触顶。

**新增加硬预算 `PROMPT_TOKEN_BUDGET = 6000` token**（中日韩一字算 1 token 的保守估算）

- 超预算时按固定优先级丢块：反例示例 → 正例示例 → 话题 → 最近说过什么 → 连发提示 →
  Core 背景 → 内心描述 → 历史 → 人设截短（`PERSONA_SHED_CHARS = 400`）。
- **不从中间硬截整段 prompt**：截在句子中间会把「输出格式」要求切掉，模型就开始自由发挥。
  任务指令、关键要求、输出格式三段永不丢。
- 丢块时打一条 info 日志（写了什么、现约多少）；真到丢无可丢仍超预算时打 warning。
- 人设在 `persona.py` 里本来就截到 800 字，但生成器不该信任调用方，所以再卡一道。
  实测把 AstrBot 人设写成 9000 字（≈7700 token）时：原本会发出约 10500 token 的请求，
  现在自动降到约 1100。
- 对方最近那句话以前会整句 500 字进 prompt（`state.py` 里 `MESSAGE_TRUNCATE_LENGTH = 500`），
  现在进 prompt 前只取前 120 字：写一句主动消息用不了那么多，多出来的全按 token 算钱。
- `debug: true` 时日志打出本次 prompt 的字符数与 token 估算。

## v1.7.5 — 接上身体（配合 Humanoid Core v2.14）

念头模型本身在 v1.7.4 已经对了，但它能看到的身体只有 `energy` 与 `social_energy` 两个标量。
所以下面这些事它根本无从判断：她是不是真的在睡、饿不饿、身上舒不舒服、以及她自己到
底有没有想说这句话。

**优先读 Core 导出的契约快照**（`roles[bid].self.contract`，带 `v` 版本号，字段只加不改）

- `core_bridge._self_view()`：读到契约时把身体轴平铺进快照（`sleep_pressure`、`sleep_debt`、
  `hunger`、`discomfort`、`arousal`、`social_desire`、`asleep`、`feelings`、`form`、`activity`）。
  读不到契约（没装 Core，或 Core 还是 v2.13.x）时逐字退回旧字段，行为与 v1.7.4 完全一致
  ——这一条有冒烟测试卡着（`abs(base - awake) < 1e-9`）。
- 按用户的好感度仍走 `users[uid].mood.affection` 旧路径；契约里只承诺路径不内嵌按用户数据
  （1000 人时会把文件撑大）。

**身体真的参与决策**（`desire.mood_multiplier(body=...)`）

- `asleep` → ×0.06：日程写着她在睡就不再发，而不是概率打折后照发。
- 睡眠压力 ≥88 → ×0.25；≥72 → ×0.55；≥55 → ×0.85。
- 躯体不适 ≥62 → ×0.55；饥饿 ≥88 → ×0.75。
- `social_desire` → ×(0.55 + 0.9·desire/100)：她自己的心思是油门也是刹车。刚聊完时念头
  根本涨不起来，一个人待久了自然涨上去。总区间从 0.05~1.15 拓宽到 0.02~1.45。

**身体约束这一句的形式**

- `form.max_chars` 与 `max_message_length` 取小：Core 说「这一轮只说一两句」，主动消息就
  真的只有一两句，不会顶到上限写成小作文。
- `feelings` + 当前过程 + 想说话的程度，编译成一句第一人称近况进 prompt（不出现数值）。

**写回信号**（新文件 `social/signals.py`）

- 发送成功后写 `last_proactive_at` / `last_target_uid`；每周期写最大冷落计数与当前欲望。
- Core 读到后：泄掉她的社交欲望（同一条信号不重复消费）、把连续被冷落变成一句体感。
- **两边各写各的文件**：本插件只写自己目录下的 `humanoid_signals.json`，Core 只读，
  超过 15 分钟不采信（社交层停了就不会继续影响身体）。
- 写盘区分「内容变了」与「心跳」：变了立即写，没变每 2 分钟只刷一次 mtime。第一版把两者
  合在一起写，导致刚变的冷落计数会被节流拖到两分钟后——已修。
- 写失败不影响发送，仅记日志。

**其它**

- `SocialState.max_no_reply_streak(bid)`：按角色取最严一次的连续没人接。
- `/自主社交状态` 显示联动到底读到了哪一层（契约 v1 / 旧字段 / 没读到）。

## v1.7.4 — 从定时器改成念头

针对「不够自然、极其僵硬」：同一个用户隔两小时被发一条一百多字的「这一天辛苦啦，该休息了」，还是在他明显一直在线说话的时候。这不是参数没调好，是机制里根本没有「想不想」这个量。

**新增 `social/desire.py`：念头（urge）模型**

- 每个人身上有一个随时间积累的量：`增速 = 在意程度 × 现在合不合适 × 我自己此刻的状态`。攒过门槛才进入候选，取代原来的「每 2-9 分钟掷一次骰子 + 概率曲线」。
- **在意程度**（`interest`，慢变量 EMA）由好感度、聊过的量、对方接不接话、被连续冷落几次合成 —— 不同人的节奏天然不同，不再靠一个全局冷却值近似。
- **刚聊过直接清零**。原来的 `RECENT_CHAT_THRESHOLD` 只挡 3 分钟，所以「两小时前才聊完又冒出一条主动消息」完全合法；现在由 `recent_talk_minutes`（默认 45）控制。
- **作息硬闸**：按小时统计每个人平时的活跃时段（带折半衰减，跟得上对方换作息），不在那些点时念头攒得极慢，而且**攒满了也不发** —— 第一版只把它做成「涨得慢」，仿真里凌晨四到六点仍漏出 27 次，所以它是闸门而不是权重。安静时段同理。
- **门槛每次重抽**（`fire_gate`）：固定门槛会让间隔精确得像周期（仿真里出现 48.0h、48.0h、48.0h），所以每次说完重新抽一个 1.0~1.55。
- 惰性积分：只在被读到时按经过的时间补算，不为每个用户挂定时器。
- 引擎与状态层共用一个可注入的时间源。之前 `try_once` 用 `time.time()`、时段判断用 `datetime.now()`，两处各取一次时钟；统一后仿真才能真跑。

**攒满不等于就发：两阶段把关**

- 规则闸门：热聊中／刚聊过／上一条还挂着没回／护栏冷却未过／刚否决过／对方说过要睡了／作息不对／安静时段。
- 然后**让模型自己判断该不该说**（`llm_gate`，新）：同一次调用输出 `SEND` + 正文，或 `NO` + 一句理由。旧流程里「要发」是抽签决定的，模型只负责填台词，所以情境多不合适它都会写出一句；现在它可以回答 NO，而这正是真人多数时候的状态。否决后念头回落而不是清零（「想过，先算了」），并进入 `skip_cooldown_minutes` 的缓长期。手动触发不走否决 —— 主人要看效果就别替他否决。

**由头先行**：从对方消息里提取时间锚点（明天／后天／今晚／周末／周X／下周／待会，以及「明天见」这类客套的排除），到点后把它当作主动开口的理由并直接算作一次念头落地；问过了就消费掉，没发出去则把锚点往后推 6 小时 —— 否则到点的由头会每个心跳拉着重试，仿真里出现过每半小时发一次同一件事。`generate_reason` 的理由优先级重排为由头 > 长时间未联系 > 时段。

**发送后的体验**：`pending_result` 记录 waiting／replied／ignored。被接话归零冷落计数、抬在意度；超时没回（判定窗口至少 12 小时，比回复率统计的 2 小时宽 —— 晚上发的话早上才回很正常）则降在意度、压低念头天花板，连着三次后没有具体由头就攒不出去。

**去掉截图里那种味道**

- 清洗括号动作与旁白：`（轻轻地回应你的拥抱…）`、`*摸摸头*`、`【窗外夜色】`（`strip_roleplay_actions`，默认开）；全删光时退回原文，不能发空消息。语C 风人格几乎每条都会这么写，而微信里没人这么打字。
- 默认去掉 emoji（`allow_emoji=false`）：每条带个❤️是典型 AI 痕迹。
- prompt 新增：不写括号动作神态、爱称一条最多出现一次、不催睡不道晚安、不出现「作为AI」自述；人格设定里的动作描写只用于理解性格。
- 没接 Core 时用在意程度估算关系档位，不再永远停在「关系未知」。

**配置默认值迁移（重要）**

AstrBot 的 `check_config_integrity` 只在**键缺失时**插默认值，已有值一律保留 —— 所以 v1.7.3 把 `user_cooldown_minutes` 从 180 调到 720、`max_message_length` 从 200 调到 60，对老用户一点作用都没有，他们实际跑的仍是 180/200，表现出来就是「两小时一条一百多字」。v1.7.4 首次启动会把仍等于旧默认值的 `global_cooldown_minutes`／`user_cooldown_minutes`／`max_message_length`／`reply_window_hours` 提升一次并 `save_config()` 写回，面板里看到的就是生效值；留一个标记文件，之后用户手动改回旧值不会被反复覆盖。

**权限：两个指令默认仅主人**

- 新增 `owne
{
  "enabled": {
    "type": "bool",
    "description": "是否启用自主社交",
    "default": true
  },
  "mode": {
    "type": "string",
    "description": "运行模式：auto 自动检测 Humanoid Core；humanoid 仅使用 Core；standalone 独立运行",
    "default": "auto"
  },
  "activity_level": {
    "type": "int",
    "description": "整体想说说话的程度（10-90）。它缩放念头的积累速度，不是「每隔几分钟发一条」：55 接近一个爱聊天的朋友，30 以下会很克制。",
    "default": 55,
    "min": 10,
    "max": 90
  },
  "urge_refill_hours": {
    "type": "int",
    "description": "【节奏】对一个最在意的人，攒满一次「想说两句」的念头大约需要多少小时。数值越小越爱找人说话；一般熟的人会自动按比例更慢。",
    "hint": "只管「另起一个新话题」。话说到一半的追问、挂着没回访的事，都走下面几条，不看这个值。",
    "default": 3,
    "min": 2,
    "max": 96
  },
  "recent_talk_minutes": {
    "type": "int",
    "description": "【节奏】刚聊完多久之内绝不另起一个话题。真人不会话刚说完又发一句无关的，这条最能去掉定时器感。",
    "hint": "不影响未完话题那几条：它们恰恰发生在刚聊完之后。",
    "default": 25,
    "min": 5,
    "max": 720
  },
  "llm_gate": {
    "type": "bool",
    "description": "发送前让模型自己判断「现在这句该不该说」，它可以说 NO（想过但不说了）。关掉则只走规则闸门（刚聊过/没回话/作息不对/说过要睡）。",
    "default": true
  },
  "skip_cooldown_minutes": {
    "type": "int",
    "description": "模型否决（想过但决定不说）之后，多久之内不再就同一个人重新纠结。",
    "default": 25,
    "min": 5,
    "max": 480
  },
  "respect_user_rhythm": {
    "type": "bool",
    "description": "按对方平时的活跃时段挑时机：没见过 TA 在这个点说过话的时候，念头攒得极慢。凌晨四点发主动消息是最强的机器人破绽。",
    "default": true
  },
  "cue_followup": {
    "type": "bool",
    "description": "记住对方话里的时间锚点（「明天面试」「周五交报告」），到点后把它当成主动开口的由头。真人大多是想起一件具体的事才说话的，没由头时更容易不发。",
    "default": true
  },
  "followup_enabled": {
    "type": "bool",
    "description": "【未完话题】这场话还没说完就断了时，主动接一句。里面自动分两种：对方答得敷衍（「还行」「就嗯」）就追问那件事本身；正聊着突然没声了就问一句在不在。这不等念头攒满——真人以分钟计。",
    "hint": "不管最后一句是谁说的。旧版要求「最后一句必须是对方的」，而主链路一回复，最后一条永远是她自己的，于是这条在实际聊天里几乎永远不成立。",
    "default": true
  },
  "probe_after_minutes": {
    "type": "int",
    "description": "【未完话题】对方回得敷衍（「还行」「就那样」「不知道」）时，隔多久追问一句关于那件事的话",
    "hint": "这条比「问在不在」更快成立：追问是有由头的，光问人呢则得像偶然。",
    "default": 3,
    "min": 1,
    "max": 120
  },
  "followup_after_minutes": {
    "type": "int",
    "description": "【未完话题】双方都不说话了，隔多久算这场话断了，接一句「在吗」类的话",
    "default": 6,
    "min": 3,
    "max": 240
  },
  "followup_max_minutes": {
    "type": "int",
    "description": "【断话跟进】超过这么久就别追了（隔两小时再问「在吗」就不像接话了）",
    "default": 90,
    "min": 15,
    "max": 720
  },
  "followup_cooldown_minutes": {
    "type": "int",
    "description": "【断话跟进】两次跟进之间的最短间隔（同一段沉默只会问一次，这个是跨段的护栏）",
    "default": 45,
    "min": 5,
    "max": 720
  },
  "loop_enabled": {
    "type": "bool",
    "description": "【回访】记住对方提过、但没说结果的事（面试、复查、搬家、跟谁吵了一架），过几小时主动问一句后来怎么样了。",
    "hint": "与「时间锚点」互补：对方说了「明天面试」走锚点（他自己给了钟点）；只说「今天面试了」走这里。",
    "default": true
  },
  "loop_min_hours": {
    "type": "float",
    "description": "【回访】最少隔多少小时再问。当时追问不礼貌，隔太久又像翻旧账。",
    "default": 2.5,
    "min": 1.0,
    "max": 24.0
  },
  "loop_max_hours": {
    "type": "float",
    "description": "【回访】最迟隔多少小时必须问出去，超过就丢掉不再提。",
    "default": 14.0,
    "min": 3.0,
    "max": 48.0
  },
  "closer_enabled": {
    "type": "bool",
    "description": "【收场】她主动找的话一直没人回时，过一阵自己冒一句把话收掉（「没事 就随口一说」），而不是从此对这个人静音。",
    "hint": "一次没人回只收一次场，内容明确不要求对方回复。",
    "default": true
  },
  "closer_after_hours": {
    "type": "int",
    "description": "【收场】她主动发的话隔多久没人回，才适合自己收尾。太短就成了「你怎么不回我」。",
    "default": 8,
    "min": 2,
    "max": 72
  },
  "track_own_replies": {
    "type": "bool",
    "description": "【拟人地基】把 bot 在聊天里自己说过的话也记进对话账本。开了之后她才知道自己上一句问了什么，追问才接得上话头。",
    "hint": "依赖 AstrBot 的 after_message_sent 钩子；流式回复分段送达时框架不触发该钩子，那部分记不到。关掉后未完话题的判定会明显变弱。",
    "default": true
  },
  "use_core_clock": {
    "type": "bool",
    "description": "【时区】安静时段与对方作息按她所在城市的小时算（需要装了 Humanoid Core v2.14.2+）",
    "hint": "没开或读不到契约时退回本机时钟。插件跑在北京时区的机器上、她设在加里宁格勒时，不换算会把她的白天当深夜、把她的深夜当白天。",
    "default": true
  },
  "global_cooldown_minutes": {
    "type": "int",
    "description": "【护栏】两次主动消息之间的最短间隔（分钟）。节奏主要由念头决定，这里只防极端情况下的刷屏。",
    "default": 20,
    "min": 5,
    "max": 1440
  },
  "user_cooldown_minutes": {
    "type": "int",
    "description": "【护栏】同一个人两次被主动联系的最短间隔（分钟）。",
    "default": 90,
    "min": 10,
    "max": 2880
  },
  "quiet_start": {
    "type": "int",
    "description": "安静时段开始小时（0-23）：这段时间里念头几乎不增长，不是「概率打折后照发」。",
    "default": 23,
    "min": 0,
    "max": 23
  },
  "quiet_end": {
    "type": "int",
    "description": "安静时段结束小时（0-23）",
    "default": 7,
    "min": 0,
    "max": 23
  },
  "private_only": {
    "type": "bool",
    "description": "是否只主动私聊",
    "default": true
  },
  "debug": {
    "type": "bool",
    "description": "输出自主社交决策日志（谁攒满了念头、为什么没说）",
    "default": false
  },
  "max_message_length": {
    "type": "int",
    "description": "单条主动消息最大长度（字）。默认 60：真人主动开一句很少超过这个长度。",
    "default": 60,
    "min": 10,
    "max": 500
  },
  "strip_roleplay_actions": {
    "type": "bool",
    "description": "去掉模型写出的括号动作与旁白，如「（轻轻为你披上薄毯）」「*摸摸头*」。语C 风人格特别容易交回这种东西，而微信里没人这么打字。",
    "default": true
  },
  "allow_emoji": {
    "type": "bool",
    "description": "是否保留 emoji。默认去掉：每条消息末尾一个❤️是很典型的 AI 痕迹。",
    "default": false
  },
  "allow_burst": {
    "type": "bool",
    "description": "连发模式：约 30% 的主动消息会自然拆成两条短句，间隔 1-3 分钟补发第二条（对方期间回话则取消）。关闭后每次只发一条。",
    "default": true
  },
  "topic_memory_count": {
    "type": "int",
    "description": "记住最近多少个话题关键词用于对话延续（0-20）",
    "default": 5,
    "min": 0,
    "max": 20
  },
  "weekend_boost": {
    "type": "bool",
    "description": "周末是否让念头涨得快一些",
    "default": true
  },
  "energy_threshold": {
    "type": "int",
    "description": "Humanoid Core 精力低于此值时念头几乎涨不动（0-100）",
    "default": 15,
    "min": 0,
    "max": 100
  },
  "social_energy_threshold": {
    "type": "int",
    "description": "Humanoid Core 社交能量低于此值时念头涨得极慢（0-100）",
    "default": 20,
    "min": 0,
    "max": 100
  },
  "adaptive_reply_rate": {
    "type": "bool",
    "description": "是否把「对方接不接话」算进在意程度。关掉后不再因被冷落而降速。",
    "default": true
  },
  "reply_window_hours": {
    "type": "int",
    "description": "主动消息发出后多久内对方回话才算「接住了」。超过这个时间没回会被记成「没接话」，下次再想找 TA 得攒更久。",
    "default": 2,
    "min": 1,
    "max": 48
  },
  "owner_only_commands": {
    "type": "bool",
    "description": "「触发社交」「自主社交状态」两个指令是否仅机器人主人（全局配置 admins_id）可用。开启时忽略下方白名单；群里被非主人触发会完全静默，不当众回应。",
    "default": true
  },
  "allowed_trigger_uids": {
    "type": "list",
    "description": "除主人外还允许谁用这两个指令（填 user_id，逐条添加）。仅在 owner_only_commands 关闭时生效。",
    "hint": "ID 可用 AstrBot 自带的 /sid 获取；旧版逗号分隔的写法仍兼容。",
    "default": []
  },
  "store_message_text": {
    "type": "bool",
    "description": "是否把用户私聊消息正文存入 state.json。关闭后仅记录条数、不留存正文（降低隐私留存），但会降低「接得上之前聊天」的拟人效果，且时间锚点跟进也会失效。",
    "default": true
  },
  "user_retention_days": {
    "type": "int",
    "description": "【性能】连续多少天没再说话的用户，其历史对话正文、话题与作息画像会被自动清理（0 = 永不清理）。发送地址、聊天条数与回复统计会保留，不影响之后继续联系 TA。",
    "default": 30,
    "min": 0,
    "max": 3650
  }
}
